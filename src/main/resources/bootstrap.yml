server:
  port: 8080
spring:
  application:
    name: limits-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: all